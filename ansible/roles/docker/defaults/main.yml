---
# Variables par défaut pour l'installation de Docker
docker_users:
  - ubuntu

docker_compose_version: "2.24.0"

docker_daemon_options:
  log-driver: "json-file"
  log-opts:
    max-size: "10m"
    max-file: "3"
  storage-driver: "overlay2"
